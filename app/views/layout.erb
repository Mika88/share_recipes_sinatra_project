<!doctype html>
<html>
  <head>
    <title>Share-Recipes-app</title>
  </head>
  <body>
    <nav>
      <% if logged_in? %>
        <a href="/users/<%=current_user.slug%>"> My Page </a> 
        <a href="/recipes"> Main Page </a> 
        <a href="/logout">  Log Out </a>
        <p>Welcome, <%=current_user.username %></p>

      <% else %>
        <a href="/signup">Sign Up</a>
        <a href="/login"> Log In </a>
      <%end %>

    </nav>
    <div class="container">

        <%=yield%>

     
    </div>

  </body>
</html>